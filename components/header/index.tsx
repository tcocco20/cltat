import React from "react";

const Header = () => {
  return (
    <header className="sticky top-0 bg-foreground text-background p-4 lg:p-6">
      Header
    </header>
  );
};

export default Header;
